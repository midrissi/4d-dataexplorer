import{C as n,o as c,c as l,a2 as a,j as t,E as s,a as o,k as i}from"./chunks/framework.B_GyVC5q.js";const p=[{key:"key",label:"Variable",width:"36%"},{key:"description",label:"Description"}],h=[{key:"`$timestamp`",description:"Current UNIX timestamp in seconds"},{key:"`$isoTimestamp`",description:"Current ISO timestamp at zero UTC"},{key:"`$faker.module.method`",description:"Any [Faker](https://fakerjs.dev/api/) method — e.g. `$faker.person.firstName`, `$faker.string.uuid`, `$faker.number.int`"}],b=JSON.parse('{"title":"Environment variables","description":"","frontmatter":{"title":"Environment variables"},"headers":[],"relativePath":"guide/environments.md","filePath":"guide/environments.md"}'),m={name:"guide/environments.md"},f=Object.assign(m,{setup(u){return(k,e)=>{const d=n("DocScreenshot"),r=n("DocTable");return c(),l("div",null,[e[0]||(e[0]=a('<h1 id="environment-variables" tabindex="-1">Environment variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment variables&quot;">​</a></h1><p>Environments hold reusable key/value pairs you can insert anywhere templated text is accepted — HTTP Client URLs and bodies, Method Executor arguments, Query Builder filters, ORDA Terminal snippets, <a href="/4d-dataexplorer/guide/browsing.html#create-entity">Create entity</a> / <a href="/4d-dataexplorer/guide/selection-tools.html#anonymize">Anonymize</a> field values, and more. Manage them in the <strong>Environments editor</strong> (<a href="/4d-dataexplorer/guide/parts.html#environments">named parts</a>).</p><p>Values resolve at <strong>execution</strong> time. Saved drafts and favourites keep the raw <code>{{variable}}</code> text.</p>',3)),t("p",null,[s(d,{src:"/screenshots/40-environments-editor.png",alt:"Environment editor"})]),e[1]||(e[1]=a('<h2 id="layers" tabindex="-1">Layers <a class="header-anchor" href="#layers" aria-label="Permalink to &quot;Layers&quot;">​</a></h2><p>Three layers merge when a template resolves. Higher layers win when the same key exists in more than one place:</p><table tabindex="0"><thead><tr><th>Priority</th><th>Layer</th><th>Where it lives</th></tr></thead><tbody><tr><td>Highest</td><td><strong>Database</strong></td><td>Named environments for the connected <code>BASEID</code></td></tr><tr><td>Middle</td><td><strong>Profile</strong></td><td>Named environments stored with the current UI profile</td></tr><tr><td>Lowest</td><td><strong>Globals</strong></td><td>Always available across profiles and databases</td></tr></tbody></table><p>Open the editor from the Status bar <strong>Env switcher</strong> → <strong>Manage…</strong>, or the command palette (<strong>Environments</strong>).</p><p>Use the scope control at the top of the editor to switch between <strong>Globals</strong>, <strong>Profile</strong>, and <strong>This database</strong>.</p>',5)),t("p",null,[s(d,{src:"/screenshots/41-environments-profile.png",alt:"Environment editor — profile scope"})]),e[2]||(e[2]=t("h3",{id:"active-environments",tabindex:"-1"},[o("Active environments "),t("a",{class:"header-anchor",href:"#active-environments","aria-label":'Permalink to "Active environments"'},"​")],-1)),e[3]||(e[3]=t("p",null,[o("Only one profile environment and one database environment can be "),t("strong",null,"active"),o(" at a time. The Status bar "),t("strong",null,"Env switcher"),o(" selects them; hover it to preview the merged variable list (secrets stay masked until you reveal them).")],-1)),t("p",null,[s(d,{src:"/screenshots/42-environments-switcher.png",alt:"Environment switcher"})]),e[4]||(e[4]=a(`<h3 id="variable-fields" tabindex="-1">Variable fields <a class="header-anchor" href="#variable-fields" aria-label="Permalink to &quot;Variable fields&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Purpose</th></tr></thead><tbody><tr><td><strong>Enabled</strong></td><td>Disabled variables are ignored during resolution</td></tr><tr><td><strong>Variable</strong></td><td>Key referenced as <code>{{key}}</code></td></tr><tr><td><strong>Initial</strong></td><td>Default value; used when you reset</td></tr><tr><td><strong>Current</strong></td><td>Live value used at resolve time (stays linked to Initial until you edit Current on its own)</td></tr><tr><td><strong>Type</strong></td><td><strong>Default</strong> or <strong>Secret</strong> (masked in the UI)</td></tr></tbody></table><p>Export / Import JSON from the editor toolbar to share environments across machines.</p><h2 id="templates" tabindex="-1">Templates <a class="header-anchor" href="#templates" aria-label="Permalink to &quot;Templates&quot;">​</a></h2><p>Write <code>{{name}}</code> in any templated field. On send/run, Data Explorer substitutes the value from the active merge map.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{{baseUrl}}/rest/{{dataclass}}</span></span>
<span class="line"><span>Authorization: Bearer {{token}}</span></span></code></pre></div><p>Unresolved keys stay as <code>{{…}}</code> so typos are visible. Templated inputs highlight known variables as chips and warn on missing ones — use <strong>Manage variables</strong> from the chip popover to jump to the editor.</p><h3 id="pipe-filters" tabindex="-1">Pipe filters <a class="header-anchor" href="#pipe-filters" aria-label="Permalink to &quot;Pipe filters&quot;">​</a></h3><p>Liquid-style filters apply left-to-right after the value is resolved (or generated):</p><table tabindex="0"><thead><tr><th>Filter</th><th>Effect</th><th>Example</th></tr></thead><tbody><tr><td><code>upper</code> / <code>lower</code></td><td>Case</td><td><code>{{name | upper}}</code></td></tr><tr><td><code>capitalize</code> / <code>title</code></td><td>First letter / each word</td><td><code>{{name | title}}</code></td></tr><tr><td><code>trim</code> / <code>ltrim</code> / <code>rtrim</code></td><td>Strip whitespace</td><td><code>{{name | trim}}</code></td></tr><tr><td><code>squeeze</code> / <code>strip_newlines</code></td><td>Collapse spaces / drop newlines</td><td><code>{{name | squeeze}}</code></td></tr><tr><td><code>snake</code> / <code>kebab</code> / <code>camel</code> / <code>pascal</code> / <code>slug</code></td><td>Identifier style</td><td><code>{{title | snake}}</code></td></tr><tr><td><code>reverse</code> / <code>initials</code> / <code>length</code></td><td>Order, initials, char count</td><td><code>{{name | reverse}}</code></td></tr><tr><td><code>unwrap</code></td><td>Strip wrapping <code>{{…}}</code></td><td><code>{{email | unwrap}}</code></td></tr><tr><td><code>replace</code> / <code>replace_first</code></td><td>Replace all or first (<code>replace:search,with</code>; one arg deletes)</td><td><code>{{name | replace:a,o}}</code></td></tr><tr><td><code>remove</code> / <code>remove_first</code></td><td>Delete substring</td><td><code>{{name | remove:_}}</code></td></tr><tr><td><code>substring</code> / <code>slice</code></td><td>Slice (<code>start</code> or <code>start,end</code>; negatives like JS)</td><td><code>{{name | substring:0,5}}</code></td></tr><tr><td><code>first</code> / <code>last</code> / <code>drop</code></td><td>Take or skip characters</td><td><code>{{name | first:3}}</code></td></tr><tr><td><code>take_before</code> / <code>take_after</code></td><td>Split on first needle</td><td><code>{{email | take_before:@}}</code></td></tr><tr><td><code>append</code> / <code>prepend</code> / <code>wrap</code></td><td>Add prefix/suffix</td><td><code>{{name | <a href="mailto:append:@example.com" target="_blank" rel="noreferrer">append:@example.com</a>}}</code></td></tr><tr><td><code>truncate</code> / <code>truncatewords</code></td><td>Length limits (<code>truncate:n</code> or <code>truncate:n,ellipsis</code>)</td><td><code>{{bio | truncate:80}}</code></td></tr><tr><td><code>pad</code> / <code>pad_start</code> / <code>pad_end</code></td><td>Pad to length</td><td><code>{{id | pad_start:4,0}}</code></td></tr><tr><td><code>repeat</code></td><td>Repeat <code>n</code> times</td><td><code>{{sep | repeat:3}}</code></td></tr><tr><td><code>default</code></td><td>Fallback when empty</td><td><code>{{nick | default:anonymous}}</code></td></tr><tr><td><code>url_encode</code> / <code>url_decode</code></td><td>Percent-encoding</td><td><code>{{q | url_encode}}</code></td></tr><tr><td><code>escape</code> / <code>unescape</code></td><td>HTML entities</td><td><code>{{html | escape}}</code></td></tr><tr><td><code>base64</code> / <code>unbase64</code></td><td>Base64</td><td><code>{{token | base64}}</code></td></tr><tr><td><code>hash</code></td><td>Digest hex (<code>md5</code>, <code>sha1</code>, <code>sha256</code>, <code>sha384</code>, <code>sha512</code>)</td><td><code>{{$faker.person.firstName | hash:md5}}</code></td></tr><tr><td><code>female</code> / <code>male</code></td><td>Gender for name/email dynamics</td><td><code>{{$faker.person.firstName | female}}</code></td></tr><tr><td><code>min</code> / <code>max</code> / <code>between</code></td><td>Numeric (or date) bounds</td><td><code>{{$faker.number.int | between:10,100}}</code></td></tr><tr><td><code>after</code> / <code>before</code></td><td>Date bounds (<code>YYYY-MM-DD</code>)</td><td><code>{{$faker.date.between | after:2020-01-01}}</code></td></tr></tbody></table><p>Unknown filters leave the whole <code>{{…}}</code> token unresolved.</p><p>Args cannot contain raw <code>{}</code> or <code>|</code>. Use quotes for commas (<code>replace:&quot;a,b&quot;,c</code>), <code>\\,</code> for a literal comma, and <code>\\x7b</code> / <code>\\x7d</code> / <code>\\u007b</code> for braces (<code>replace:\\x7b\\x7b</code> deletes leftover <code>{{</code>).</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{{email | unwrap}}</span></span>
<span class="line"><span>{{email | replace:\\x7b\\x7b | replace:\\x7d\\x7d}}</span></span>
<span class="line"><span>{{$this.firstname | kebab}}.{{$this.lastname | kebab}}@example.com</span></span>
<span class="line"><span>{{$faker.person.fullName | female | upper}}</span></span>
<span class="line"><span>{{$faker.person.firstName | hash:md5}}</span></span>
<span class="line"><span>{{$faker.number.int | min:1 | max:50}}</span></span>
<span class="line"><span>{{$faker.date.between | between:2024-01-01,2024-12-31}}</span></span></code></pre></div><h3 id="this-call-context" tabindex="-1"><code>$this</code> (call context) <a class="header-anchor" href="#this-call-context" aria-label="Permalink to &quot;\`$this\` (call context)&quot;">​</a></h3><p>Reserved paths that read the <strong>current execution object</strong>. They are not stored in environments and cannot be overridden by env keys.</p><table tabindex="0"><thead><tr><th>Surface</th><th>Examples</th></tr></thead><tbody><tr><td>Entity create/edit</td><td><code>{{$this.firstName}}</code>, <code>{{$this.email | lower}}</code></td></tr><tr><td>HTTP Client</td><td><code>{{$this.method}}</code>, <code>{{$this.path}}</code>, <code>{{$this.url}}</code>, <code>{{$this.headers.Authorization}}</code>, <code>{{$this.params.q}}</code>, <code>{{$this.body.raw}}</code></td></tr><tr><td>Method Executor</td><td><code>{{$this.methodName}}</code>, <code>{{$this.scope}}</code>, <code>{{$this.dataClass}}</code>, <code>{{$this.parent}}</code>, <code>{{$this.key}}</code>, <code>{{$this.args.0}}</code></td></tr><tr><td>Query Builder</td><td><code>{{$this.dataclass}}</code>, <code>{{$this.filter}}</code>, <code>{{$this.entitySetId}}</code>, <code>{{$this.params.1}}</code></td></tr></tbody></table><p>Dotted paths walk objects (header names match case-insensitively) and arrays (numeric segments). Sibling fields that reference each other resolve in multiple passes (e.g. <code>email: {{$this.firstName | lower}}@example.com</code>). Cyclic <code>$this</code> references stay unresolved. An exact leaf <code>{{$this.vector}}</code> in a JSON payload rehydrates to the typed value.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{{$this.firstName | lower}}.{{$this.lastName | lower}}@example.com</span></span>
<span class="line"><span>Authorization: {{$this.headers.Authorization}}</span></span>
<span class="line"><span>{{$this.method}} {{$this.path}}</span></span></code></pre></div><h2 id="dynamic-variables" tabindex="-1">Dynamic variables <a class="header-anchor" href="#dynamic-variables" aria-label="Permalink to &quot;Dynamic variables&quot;">​</a></h2><p>Keys that start with <code>$</code> are <strong>dynamic variables</strong>. They are not stored in an environment — each resolve generates a fresh value via <a href="https://fakerjs.dev/" target="_blank" rel="noreferrer">Faker</a>.</p><h3 id="faker-paths" tabindex="-1">Faker paths <a class="header-anchor" href="#faker-paths" aria-label="Permalink to &quot;Faker paths&quot;">​</a></h3><p>Call any Faker module method with <code>{{$faker.module.method}}</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{{$faker.person.fullName}}</span></span>
<span class="line"><span>{{$faker.airline.airline}}</span></span>
<span class="line"><span>{{$faker.food.dish}}</span></span>
<span class="line"><span>{{$faker.string.uuid}}</span></span>
<span class="line"><span>{{$faker.number.int | min:1 | max:50}}</span></span>
<span class="line"><span>{{$faker.person.firstName | female}}</span></span>
<span class="line"><span>{{$faker.date.between | after:2024-01-01 | before:2024-12-31}}</span></span></code></pre></div><p>Pipe filters (<code>female</code> / <code>male</code>, <code>min</code> / <code>max</code> / <code>between</code>, <code>after</code> / <code>before</code>) map onto Faker options when the method supports them.</p><p><code>{{$faker.company.logo}}</code> and <code>{{$faker.image.personPortrait}}</code> are <strong>local SVG data URIs</strong> (not CDN URLs). Pipe <code>female</code> / <code>male</code> on portraits. Use them on Generate / Create image fields so bulk <code>$upload</code> stays small and offline.</p><h3 id="helper-templates-lists-objects" tabindex="-1">Helper templates (lists &amp; objects) <a class="header-anchor" href="#helper-templates-lists-objects" aria-label="Permalink to &quot;Helper templates (lists &amp; objects)&quot;">​</a></h3><p>Ergonomic keys for picking from lists, building arrays, and constructing JSON objects. Nested generators use <strong>bare</strong> <code>$faker…</code> paths in filter args (no nested <code>{{…}}</code>):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{{$pick | from:draft,published,archived}}</span></span>
<span class="line"><span>{{$pick | from:$lists.companyKeys}}</span></span>
<span class="line"><span>{{$sample | from:a,b,c,d | count:2}}</span></span>
<span class="line"><span>{{$sample | from:a,b,c,d | count:2,4}}</span></span>
<span class="line"><span>{{$unique | from:a,b,c,d | count:&gt;=2}}</span></span>
<span class="line"><span>{{$repeat | of:$faker.person.firstName | count:5}}</span></span>
<span class="line"><span>{{$repeat | of:$faker.person.firstName | count:2,5}}</span></span>
<span class="line"><span>{{$repeat | of:$faker.person.firstName | count:&lt;=4}}</span></span>
<span class="line"><span>{{$object | name:$faker.person.fullName | email:$faker.internet.email | status:draft}}</span></span>
<span class="line"><span>{{$vector | dims:8}}</span></span>
<span class="line"><span>{{$vector | dims:384 | normalize | min:-1 | max:1}}</span></span></code></pre></div><p><code>from</code> accepts either a <strong>literal</strong> comma-separated list (<code>from:a,b,c</code>) or a <strong>named list</strong> (<code>from:$lists.&lt;name&gt;</code>).</p><h3 id="pick-lists-this-database" tabindex="-1">Pick lists (this database) <a class="header-anchor" href="#pick-lists-this-database" aria-label="Permalink to &quot;Pick lists (this database)&quot;">​</a></h3><p>Under <strong>Environments → This database</strong>, declare named <code>$lists</code> sources (dataclass + attribute). Declarations persist with the connected database; distinct values are <strong>not</strong> stored — they load on demand the first time Anonymize (or another caller) references <code>{{$pick | from:$lists.name}}</code>. Use <strong>Refresh</strong> in the pick-lists panel to reload a source.</p><p>Missing or empty <code>$lists.&lt;name&gt;</code> leaves the helper unresolved.</p><p><code>count</code> accepts a fixed length (<code>count:3</code>), an inclusive range (<code>count:2,5</code>), or a one-sided bound (<code>count:&gt;=2</code>, <code>count:&gt;2</code>, <code>count:&lt;=4</code>, <code>count:&lt;5</code>). Open lower bounds default the upper end to 10; open upper bounds start at 1. For <code>$sample</code> / <code>$unique</code>, the range is clamped to the <code>from</code> list length. <code>$vector</code> builds a float array for embedding-style fields (<code>dims:n</code> or <code>count:n</code>, max 8192). Optional <code>normalize</code> L2-normalizes the vector; <code>min</code> / <code>max</code> bound each component (default <code>-1</code>…<code>1</code>). The same filters work on <code>$faker.helpers.arrayElement</code>, <code>arrayElements</code>, <code>multiple</code>, <code>uniqueArray</code>, and <code>weightedArrayElement</code> (e.g. <code>{{$faker.helpers.weightedArrayElement | from:a:3,b:1}}</code>).</p><p>In JSON / entity payloads, a leaf that is <strong>exactly</strong> one <code>$object</code> / <code>$sample</code> / <code>$unique</code> / <code>$repeat</code> / <code>$vector</code> template rehydrates to a real object or array (not a JSON string).</p><h3 id="trusted-folders-files" tabindex="-1">Trusted folders (<code>$files</code>) <a class="header-anchor" href="#trusted-folders-files" aria-label="Permalink to &quot;Trusted folders (\`$files\`)&quot;">​</a></h3><p>On desktop/mobile, <a href="/4d-dataexplorer/guide/trusted-folders.html">trusted folders</a> expose local files as <code>{{$files.alias}}</code> (random), <code>{{$files.alias/file.jpg}}</code> (specific), or <code>{{$files.alias/**/*.png}}</code> (glob). These resolve to a real file for HTTP Client binary / form-data bodies, entity image/blob <code>$upload</code>, and Anonymize image fields — not as strings in URLs. The explorer copies those templates with click-to-copy (tooltip becomes <strong>Copied!</strong>).</p><h3 id="clock-aliases" tabindex="-1">Clock aliases <a class="header-anchor" href="#clock-aliases" aria-label="Permalink to &quot;Clock aliases&quot;">​</a></h3><p>Wall-clock values that are not Faker methods:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{{$timestamp}}</span></span>
<span class="line"><span>{{$isoTimestamp}}</span></span></code></pre></div><h3 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to &quot;Reference&quot;">​</a></h3>`,40)),s(r,{columns:i(p),rows:i(h),variant:"meta",caption:"Dynamic variables"},null,8,["columns","rows"]),e[5]||(e[5]=a(`<h2 id="orda-terminal" tabindex="-1">ORDA Terminal <a class="header-anchor" href="#orda-terminal" aria-label="Permalink to &quot;ORDA Terminal&quot;">​</a></h2><p>In the terminal, embed <code>{{variable}}</code> in snippet source (substituted before run), call <strong><code>faker</code></strong> directly (same instance as <code>{{$faker…}}</code>), or use the <code>app.environment</code> API:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">faker.person.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">firstName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">faker.string.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uuid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.environment.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;baseUrl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.environment.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;abc&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.environment.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">use</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Local&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.environment.globals.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;apiKey&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.environment.profile.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;abc&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.environment.base.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;baseUrl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Dot commands: <code>.env</code> (show / switch active), <code>.envs</code> (list profile and database environments).</p><p>See also <a href="/4d-dataexplorer/guide/terminal.html">ORDA Terminal</a> and <a href="/4d-dataexplorer/guide/flows.html">Flows</a> (templated block fields).</p><hr>`,6))])}}});export{b as __pageData,f as default};
